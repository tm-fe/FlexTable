language: node_js

sudo: false

node_js:
- 11

before_install:
- |
    if ! git diff --name-only $TRAVIS_COMMIT_RANGE | grep -qvE '(\.md$)|(^(docs|examples))/'
    then
      echo "Only docs were updated, stopping build process."
      exit
    fi
    npm install
script:
- |
    if [ "$TEST_TYPE" = test ]; then
      npm test
    else
      npm run $TEST_TYPE
    fi
env:
  matrix:
  - TEST_TYPE=test

branches:
  only:
    - master
    - dev

notifications:
   webhooks: https://oapi.dingtalk.com/robot/send?access_token=af06d34dc153006f5dc4175296064662f00901c2d24fada39fdeac4bffd33822